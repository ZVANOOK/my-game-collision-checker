package main.java.com.mygame;

// Имитация проверки коллизии (для примера)
class CollisionDetector {
    public static boolean checkCollision(GameObject object1, GameObject object2) {
        // Логика проверки коллизии
        return Math.random() > 0.5; // заглушка
    }
}
